<UserControl x:Class="MentalMathGame.Views.BadgesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Barre supérieure -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" Padding="24,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                        Content="← Retour"
                        Style="{StaticResource DangerButtonStyle}"
                        Command="{Binding BackCommand}"
                        Padding="8,4" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1"
                           Text="Badges"
                           FontSize="18" FontWeight="Bold"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Progression -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}"
                BorderThickness="0,1,0,0"
                Padding="24,12">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="{Binding ProgressText}"
                           FontSize="14" FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           Foreground="{StaticResource AccentBrush}"/>
                <ProgressBar Maximum="{Binding TotalCount}"
                             Value="{Binding UnlockedCount}"
                             Height="6" Width="300"
                             Margin="0,8,0,0"
                             Style="{StaticResource TimerBarStyle}"/>
            </StackPanel>
        </Border>

        <!-- Liste des badges -->
        <ScrollViewer Grid.Row="2" Margin="24,16,24,16" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Badges}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="2"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="6" CornerRadius="8" Padding="20,16">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="{StaticResource CardBrush}"/>
                                    <Setter Property="Opacity" Value="0.45"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsUnlocked}" Value="True">
                                            <Setter Property="Background" Value="#1A3A5C"/>
                                            <Setter Property="Opacity" Value="1"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <!-- Statut -->
                                <TextBlock FontSize="11" FontWeight="Bold" Margin="0,0,0,6">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="Verrouillé"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsUnlocked}" Value="True">
                                                    <Setter Property="Text" Value="Debloque"/>
                                                    <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <!-- Nom -->
                                <TextBlock Text="{Binding Name}"
                                           FontSize="15" FontWeight="Bold"
                                           Margin="0,0,0,4"/>

                                <!-- Description -->
                                <TextBlock Text="{Binding Description}"
                                           FontSize="12"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           TextWrapping="Wrap"/>

                                <!-- Date de déblocage -->
                                <TextBlock FontSize="11" Margin="0,8,0,0"
                                           Foreground="{StaticResource AccentBrush}"
                                           Visibility="{Binding IsUnlocked,
                                                       Converter={StaticResource BoolToVisibilityConverter}}">
                                    <TextBlock.Text>
                                        <Binding Path="UnlockedAt" StringFormat="Obtenu le {0:dd/MM/yyyy}"/>
                                    </TextBlock.Text>
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
